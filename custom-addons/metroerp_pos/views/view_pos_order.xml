<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_pos_order_form_inherit" model="ir.ui.view">
        <field name="model">pos.order</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_pos_form" />
        <field name="arch" type="xml">
            <xpath expr="//page[@name='payments']//field[@name='payment_ids']" position="before">
                <button
                    name="%(action_pos_payment_change_wizard)d"
                    context="{'pos_session_id' : session_id}"
                    string="Change Payments"
                    type="action"
                    states="paid,done"
                    groups="point_of_sale.group_pos_manager,point_of_sale.group_pos_user"
                />
                <br/>
            </xpath>
            <xpath expr="//header" position="inside">
                <button name="open_cancel_wizard" string="Cancel" type="object" states="paid,done"/>
            </xpath>
        </field>
    </record>
</odoo>
