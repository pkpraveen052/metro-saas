<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <record id="inherited_view_calendar_event_form" model="ir.ui.view">
            <field name="name">calendar.event.form.inherit</field>
            <field name="model">calendar.event</field>
            <field name="inherit_id" ref="calendar.view_calendar_event_form"/>
            <field name="arch" type="xml">
                <xpath expr="//div[hasclass('oe_title')]" position="after">
                    <label for="company_id" string="Company" class="oe_edit_only"/><br/>
                    <field name="company_id" class="oe_inline" groups="base.group_multi_company"/>
                </xpath>
            </field>
        </record>

        <record id="inherited_view_calendar_event_tree" model="ir.ui.view">
            <field name="name">calendar.event.tree.inherit</field>
            <field name="model">calendar.event</field>
            <field name="inherit_id" ref="calendar.view_calendar_event_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='duration']" position="after">
                    <field name="company_id" groups="base.group_multi_company"/>
                </xpath>
            </field>
        </record>

        <record id="inherited_view_calendar_event_type_tree" model="ir.ui.view">
            <field name="name">calendar.event.type.tree.inherit</field>
            <field name="model">calendar.event.type</field>
            <field name="inherit_id" ref="calendar.view_calendar_event_type_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='name']" position="after">
                    <field name="company_id" groups="base.group_multi_company"/>
                </xpath>
            </field>
        </record>

    </data>
</odoo>
